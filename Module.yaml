displayName: 'JSON Stringify'       # Display name for the module that will be displayed in the UI.
moduleName: 'json-stringify'        # Module name that will be used to reference the module, and uniquely identify the module and their versions.
description: 'Accepts an arbitrary JSON object, and returns the stringified version.'       # Module description that will be displayed in the UI.
versionName: 'v1.0.0'                    # Module version name that will be used to reference the module version, and uniquely identify the module version with a common name.
isPublic: true                          # Boolean value that indicates if the module is Public or Private.
categories:                             # Array of category that will be used to group the module in the UI.
  - 'Experimental'
type: 'Processing'      # Module type (Input, Processing, Output) that will be used to group the module in the UI.
image:
  name: 'weevenetwork/json-stringify'     # Image name from the docker registry.
  tag: 'v1.0.0'                               # Image tag from the docker registry.
  homepage: 'https://hub.docker.com/r/weevenetwork/json-stringify'      # Image homepage from the docker registry.
repository: 'https://github.com/weevennetwork/json-stringify'     # Repository URL to source code.
envs:                                   # Array of environment variables that will be used to configure the module. Also used to show related fields on the UI.
  - name: Output Label                   # name of the field to be displayed on the UI.
    key: OUTPUT_LABEL                # key to set in the environment variables.
    description: The output label of the resulting string     # Description/helper text for the field.
    value: 'message'                         # Value to be set in the environment variables.
    type: 'text'                    # Type of the field. Possible values are: ['text', 'select', 'boolean', 'date', 'time', 'dateTime-local', 'number', 'multiple', 'CSV', 'file']
    options: []                        # Options for the field if it is a select or multiple select. An array of string options
    dependencies: []                    # List of dependencies for the field. This field is only displayed if all the dependencies are met. If
ports: []
envPorts: []
mounts: []
envMounts: []
devices: []
envDevices: []
tags:                                   # Array of tags.
  - 'Python'
  - 'Message-Composer'
  - 'Write-Message'
  - 'Alert'
icon: 'https://some.url/to/icon.png'    # Icon URL that will be displayed in the UI.
